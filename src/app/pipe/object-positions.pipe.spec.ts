import { ObjectPositionsPipe } from './object-positions.pipe';

describe('ObjectPositionsPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjectPositionsPipe();
    expect(pipe).toBeTruthy();
  });
});
